endpoint: https://eu1.prisma.sh/kumar-abhirup-c1dbf1/kumarabhirup-backend/dev
datamodel: datamodel.prisma

generate:
  - generator: graphql-schema
    output: ./src/generated/

hooks:
  post-deploy:
    # - graphql get-schema -p prisma
    - prisma generate