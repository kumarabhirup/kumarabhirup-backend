# import * from './generated/prisma.graphql'

type Mutation {
    sendMessage(fullname: String!, phone: String, email: String!, subject: String!, message: String!): Contact!
}

type Query {
    contacts: [Contact]!
}